# Row 9 review

The final coherent row-9 source registered successfully with the final assembler transform (`scale: 0.6513157894736842`). It preserves the repaired image-right `090` profile while making `000` visibly skyward and easing the late-row return to frontal down. At normal pet size, all eight replacement cells are complete, share the planted lower-body anchor, and remain within their cells.

- `000` pass: eyes and face aim up.
- `022.5` pass: first upper-right intermediate; small right cue follows the upward pose.
- `045` pass: clear upper-right quadrant; head and eyes advance right.
- `067.5` pass: near-right pose; rightward profile cue increases continuously.
- `090` pass: unmistakable screen-right nose/eye/head profile, using the approved screen-right cardinal anchor as the direct reference.
- `112.5` pass: the rightward profile begins turning back toward frontal while lowering.
- `135` pass: clear lower-right quadrant with a wider face and both eyes beginning to return.
- `157.5` pass: nearly frontal lower-right pose flows evenly into the `180` down cardinal without a row-boundary snap.

The sword remains attached and follows the established running-right screen position while the profile turns; no whole-sprite rotation, replacement eyes, overlap, clipping, or detached effects are visible.
