# Changelog

## 1.4.7 — 2026-07-17

### 修复

- Miku 主题任务页会随 Codex 深色外观切换完整文档 token；Markdown、TeX、CodeMirror、Monaco、终端和 review diff 不再继承浅色纸面
- Miku 专用运行时变量在夜间改用深色背景和高对比前景，避免主题配置中的浅色文本/背景覆盖官方深色状态
- 实时 DOM 审计同时校验浅色和深色编辑器前景、背景与预格式文本 token

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或模型供应商配置

## 1.4.6 — 2026-07-17

### 改进

- 恢复 Codex 官方原生 Pet 浮层，让官方气泡/通知托盘负责对话状态展示，主题不再注入自定义气泡
- 设置页尺寸验证改为官方 `input#pet-size` 控件；主题不再创建自定义 Pet 尺寸卡片或本地尺寸状态
- 实时审计和回归测试同步检查原生浮层模式与官方尺寸控件

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或模型供应商配置

## 1.4.5 — 2026-07-17

### 修复

- 设置页面不再隐藏主题内透明 Pet；设置页可以继续看到和拖动 Pet
- 新增 `80px–220px` Pet 尺寸控制，大小即时生效并保存在本机浏览器存储中
- 实时审计增加设置页 Pet 显示、尺寸控件和尺寸范围检查

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或模型供应商配置

## 1.4.4 — 2026-07-17

### 修复

- 为主题内透明 Miku Pet 增加运行态、等待确认和审阅态对话气泡
- 气泡文案跟随 Codex 系统语言，空闲时自动隐藏，并保持透明背景与可拖动 Pet 互不干扰
- 热注入时会为已有 Pet 节点补齐气泡结构，不需要重启 Codex

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或模型供应商配置

## 1.4.3 — 2026-07-17

### 修复

- 修复 New chat 首页错误隐藏透明 Miku Pet 的问题；首页现在和 Hero、建议卡片同时保留 Pet
- 只有设置页或展开的辅助模块会隐藏 Pet，避免遮挡真实控件

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或模型供应商配置

## 1.4.2 — 2026-07-17

### 修复

- 修复热注入后旧 Pet 动画控制器没有被销毁，多个计时器同时改写同一精灵导致的对话闪烁
- Pet 控制器现在会注册、同步到全局状态并在重新注入或切换页面时可靠销毁，实时审计要求可见页面仅保留一个控制器
- 将精灵帧切换改为裁剪视口内的 GPU `transform` 位移，并放缓运行态节奏，减少透明大图重复重绘造成的闪烁
- 对运行态按钮短暂消失增加 1.6 秒稳定窗口，避免流式响应期间 Pet 在运行和空闲动画之间来回跳变

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或模型供应商配置

## 1.4.1 — 2026-07-17

### 修复

- Composer 主操作按钮根据 Codex 原生状态显示发送箭头、停止方块或暂停双竖线，运行中不再误显示发送图标
- 监听原生 `aria-label` 状态切换，任务开始和结束后按钮图标即时同步
- 运行态按钮使用独立的粉色交互样式，避免暂停/停止操作与发送操作混淆

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或模型供应商配置

## 1.4.0 — 2026-07-17

### 新增

- Miku Future Pet 改为主题窗口内的透明动画图层，支持空格/回车挥手、拖动和位置记忆
- 项目对话页保留 Pet，New task、设置页以及摘要、侧栏、底部模块展开时自动隐藏
- 主题启动时关闭原生浮动 Pet，避免系统 BrowserWindow 阴影和黑色模糊背景覆盖内容

### 修复

- 主题注入器不再监听自己写入的 DOM 变化，降低路由切换、流式输出时的闪烁和卡顿
- 观察器改为按相关节点合并调度，消息流和编辑器文本变化不再触发整页装饰扫描
- 动画只更新 Pet 精灵坐标，并暂停不可见页面的高频帧更新
- 实时审计兼容新版 Codex 精简为三个顶部导航入口的侧栏结构

### 安全

- 仍只通过本机回环 CDP 关闭原生浮动 Pet 并注入主题，不修改官方 `.app`、`app.asar`、
  签名、认证或模型供应商配置

## 1.3.5 — 2026-07-17

### 修复

- New task 首页锁定为单屏构图，透明人物越界层不再把页面撑出纵向滚动区域
- 项目对话中的右下角 Miku 小头像会在固定摘要、右侧面板、底部面板或右侧
  浮动模块展开时自动隐藏；模块收起后恢复显示
- 实时 DOM 审计与注入验证新增首页滚动锁和模块遮挡状态检查

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.3.4 — 2026-07-17

### 修复

- 设置页现在按 Codex 当前外观自动切换完整的白天与夜间色板，修复白色页面、
  黑色卡片和深色文字混用导致的低对比度问题
- 设置侧栏、权限卡片、下拉菜单、链接、开关和表单控件共享同一套模式变量，
  切换系统或 Codex 外观后会自动同步
- 注入器与实时 DOM 审计支持设置页结构，并校验设置卡片文字对比度不低于 4.5

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.3.3 — 2026-07-17

### 改进

- 移除侧栏左下角无实际功能的 Setup / 继续设置进度卡，为项目和任务列表释放空间
- 保留底部原生用户菜单、Miku 小头像和账户状态，不改变任何 Codex 功能

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.3.2 — 2026-07-17

### 修复

- 侧栏顶部导航只保留 Codex 原生文字，修复切换 New task、Scheduled、Plugins、
  Pull requests 和 Chat 时主题标签与原生标签短暂叠加产生的文字重影
- New task 不再永久显示选中背景；各菜单完全跟随 Codex 原生
  `aria-current`、`aria-pressed` 和当前行状态切换高亮

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.3.1 — 2026-07-17

### 修复

- 修复进入项目对话后右下角 Miku 小头像被首页专用显示规则隐藏的问题
- 对话页头像缩小并向内收，避开输入框和右侧面板；窄窗口下也保持显示

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.3.0 — 2026-07-16

### 新增

- 主题配置支持独立的主界面壁纸、Hero 场景、透明人物层和四张卡片 PNG 图标
- 当 Codex 当前状态不提供原生建议卡片时，生成四个可交互的替代按钮；原生按钮
  恢复后会自动转发点击，保持功能一致

### 改进

- Miku 首页按参考图扩大 Hero，将人物放大并移动到中央，卡片横跨 Hero 底部
- 人物拆成卡片后层与头发前层，越过 Hero 边界但不遮挡卡片图标和文字
- New task 与对话页统一使用同一张主界面壁纸，Hero 内部单独使用无人物场景
- 主题文字和字体继续跟随 Codex 当前系统语言及原生字体

### 修复

- 修复不同项目状态下原生建议按钮被 Hero 背景覆盖、四张卡片偶发消失的问题
- 保持 Markdown、TeX、文件编辑器、review diff 和终端的独立可读性色板

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.2.1 — 2026-07-16

### 改进

- 按 Miku 参考图重构首页主视觉：角色按高度缩放，保留完整头部和上半身，
  四个原生建议按钮叠放到横幅底部，不再被横幅裁切隐藏
- 增加人物前景遮罩与轻微视差错位：头发越过横幅上边框，人物穿过第三张卡片，
  第四张卡片再压回前景；右下角增加倾斜 Miku 拍立得形成三段景深
- 为顶部栏、输入框、侧栏、Outputs 和分段操作按钮补齐青粉玻璃态、悬停态、
  聚焦态和禁用态

### 修复

- 补齐 CodeMirror、文件编辑器、review diff、Markdown/TeX 预格式文本和
  VS Code 编辑器 token，修复浅色画布继承暗色白字导致内容不可读
- 终端改为独立深青底配浅色字，避免浅色 Miku 外壳下出现白底白字

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.2.0 — 2026-07-16

### 新增

- 增加 `miku-pastel` 精准预设与无假 UI 的纯背景素材
- 增加 `Configure Miku Theme.command` 和
  `scripts/configure-miku-preset-macos.sh`，可只保存主题而不重启 Codex
- 主题配置支持 `preset`、结构色、强调色、正文色和弱化色

### 改进

- 按 Codex `26.707.91948` 的实际 DOM 补齐任务页
  `.app-shell-main-content-*`、`.thread-scroll-container`、Outputs/Sources
  浮层、用户消息、代码块和输入框样式
- 使用 Codex 自身的 `--color-token-*` 变量统一明亮青粉配色，减少对易变
  CSS 模块类名的依赖
- 主页放大为独立角色横幅，左侧保留文字负空间；任务页增加高强度浅色遮罩，
  避免角色图与消息正文争夺视觉焦点
- 测试不再错误要求安装时强制 `appearanceTheme = "dark"`
- `HOME` 缺失时增加 `dscacheutil` 回退，避免 `dscl` 在受限环境失败后直接退出
- `doctor` 回归测试使用隔离的临时 HOME，不再受当前 Codex 页面或残留运行状态影响

### 安全

- 仍只通过本机回环 CDP 注入，不修改官方 `.app`、`app.asar`、签名、认证或
  模型供应商配置

---

## 1.1.1 — 2026-07-16

### 修复

- 不再用 `launchctl submit` 托管带调试口的 Codex：退出 SwiftBar / 关掉 Codex 后不应再被 launchd 自动拉起
- 暂停与完全恢复时清理 `com.openai.codex-dream-skin-studio.app` 作业

---

## 1.1.0 — 2026-07-16

### 新增

- SwiftBar 菜单栏入口（`Install Menu Bar.command`）：应用 / 暂停 / 换图 / 切换已保存主题 / 从图片文件夹加载 / 完全恢复
- 主题库（`themes/`）与图片投放目录（`images/`）动态加载，不再把 README 图库合成图当背景素材
- 按 Codex 应用浅色 / 深色自动切换皮肤壳（`data-dream-shell`）

### 改进

- CDP 已就绪时热切换主题（重启 injector + 短时注入），换图更快
- 注入校验放宽（项目选择器等可选），避免误杀已生效皮肤
- 注入守护优先 `nohup`；暂停状态与路径大小写下停止逻辑更稳
- 安装时不再强制 `appearanceTheme=dark`，只备份桌面外观相关键，便于恢复与自动适配

### 视觉

- 以原版暗色 portal CSS 为结构底，叠加 light 壳与更薄横幅遮罩，减轻「换图看不清」
- 示例纯横幅：`docs/images/banner-arina-hashimoto-pure-no-ui.png`（无人机 UI 合成）

### 说明

- `docs/images/gallery/` 仅为效果预览，不要当 `theme` 背景导入

---

## 1.0.0 — 2026-07-15

- 发布 macOS 通用主题制作器，而不是固定角色皮肤。
- 加入 Finder 选图、自动 JPEG 转换、主题命名和高级配色参数。
- 主页使用独立横幅，任务页使用背景与磨砂层，完整保留原生交互。
- 改为复用并验证 Codex 官方签名 Node.js，不再附带大型运行时或依赖全局 Node。
- 增加独立安装目录、桌面启动/定制/验证/恢复入口。
- 增加官方签名、CDP 端口归属、PID 身份、刷新重注入和真实 DOM 自检。
- 增加原子配置备份、精确恢复、静态测试、安装恢复循环和发布打包脚本。
- 清理固定角色内部命名；传送门主题仅作为可替换示例素材。
- 开源树：示例横幅改为无角色抽象几何图；验收截图不入库；补充 NOTICE / README 商标与安全边界说明。
